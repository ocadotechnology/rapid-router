name: CI
on:
  push:
    branches:
      - master
  pull_request:
jobs:
  tests:
    name: Run tests
    if: ${{ github.event.pusher.email != 'action@github.com' }}
    runs-on: ubuntu-18.04
    env:
      LANG: C.UTF-8
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.6.7'
          architecture: 'x64'
      - name: Install pip requirements
        run: |
          pip install pipenv
          pip install pytest-cov
          pipenv install --dev --system
          pip uninstall --no-input codeforlife-portal
          pip uninstall --no-input cfl-common
          pip install "git+https://github.com/ocadotechnology/codeforlife-portal.git@8cb2168aa57cfbb074367202baea127fa5967f8c#egg=codeforlife-portal"
          pip install "git+https://github.com/ocadotechnology/codeforlife-portal.git@8cb2168aa57cfbb074367202baea127fa5967f8c#egg=cfl-common&subdirectory=cfl_common"
          pip freeze
      - name: Collect static
        run: python example_project/manage.py collectstatic --noinput
      - name: Run tests
        env:
          SELENIUM_WEBDRIVER: chrome-headless
        run: pytest --cov=. --cov-report=xml
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v1
